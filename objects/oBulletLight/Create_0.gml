lightColor = c_yellow;
glowSize = 1;

objDir = point_direction(x, y, crosshair.x, crosshair.y);

image_angle = point_direction(x, y, crosshair.x, crosshair.y);
direction = point_direction(x, y, crosshair.x, crosshair.y);